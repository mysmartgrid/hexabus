# -*- mode: cmake; -*-

message( "===>>> CPACK_DEBIAN_PACKAGE_ARCHITECTURE: ${CPACK_DEBIAN_PACKAGE_ARCHITECTURE}")

if(CPACK_GENERATOR MATCHES "DEB")
  set(CPACK_SYSTEM_NAME ${CPACK_DEBIAN_PACKAGE_ARCHITECTURE})
  # set(CPACK_DEBIAN_PACKAGE_CONTROL_EXTRA "")
  set(CPACK_PACKAGE_FILE_NAME "${PROJECT_NAME}_${CPACK_PACKAGE_VERSION}-1_${CPACK_DEBIAN_PACKAGE_ARCHITECTURE}")
endif()

if(CPACK_GENERATOR MATCHES "RPM")
  set(CPACK_SYSTEM_NAME ${CPACK_DEBIAN_PACKAGE_ARCHITECTURE})
  # set(CPACK_DEBIAN_PACKAGE_CONTROL_EXTRA "")
  set(CPACK_PACKAGE_FILE_NAME "${PROJECT_NAME}_${CPACK_PACKAGE_VERSION}-1.${CMAKE_SYSTEM_PROCESSOR}")
endif()
