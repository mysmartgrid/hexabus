endpoint e0(1) : bool (broadcast, write);

device d1(::2) : e0;
device d2(::3) : e0;

machine class c(device[] dev) {
	state init {
		on update from dev.e0 {
			dev.e0 = true;
		}
	}
};

machine m1 : c([d1, d2]);
machine m2 : c([d1, d2, d2]); //#expect: error: duplicate entry 'd2' in device list
