# in this example, we just want to rename a device in our network
# i.e. change the names under which it appears in hexadroid and other tools

device new_device {
  ip abcd::50:c4ff:fe04:8abc;
  eids { }
}

# when this program is compiled and uploaded, it will asign a new name to the device.
# Everything else, including a state machine which could have been installed on the
# device previously, remains unchanged.
